# python library files
SET(PYTHON_PACKAGE_PATH ${SC3_PACKAGE_PYTHON_LIB_DIR})
INSTALL(DIRECTORY mert DESTINATION ${PYTHON_PACKAGE_PATH}
	PATTERN ".svn" EXCLUDE
	PATTERN "CMakeLists.txt" EXCLUDE
	PATTERN "*~" EXCLUDE
	PATTERN "*.pyc" EXCLUDE)

